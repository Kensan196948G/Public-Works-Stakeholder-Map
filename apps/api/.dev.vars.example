# wrangler dev 用ローカル環境変数の見本（実値・秘密値は書かない）
# 使い方: このファイルを .dev.vars にコピーして値を設定する（.dev.vars は gitignore 済み）
# 注: Workers デプロイは廃止（2026-08-30）。ローカル実行は `node --env-file=.env <tsx src/dev-server.ts>` を使用
APP_ENV = "local"
# DATABASE_URL は設定しない（未設定 = fixture モード）。DB モード検証時のみローカル PostgreSQL の値を設定する
