{
  "name": "@pwsm/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers API — 候補検索・メタデータ・ヘルスチェック",
  "scripts": {
    "build": "tsc -b",
    "dev": "wrangler dev",
    "deploy:preview": "wrangler versions upload --env preview"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@pwsm/contracts": "*",
    "@pwsm/domain": "*",
    "@pwsm/fixtures": "*",
    "hono": "^4.12.30",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@hono/node-server": "^2.0.10",
    "@types/node": "^26.1.1"
  }
}
