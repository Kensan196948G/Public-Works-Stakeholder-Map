# 🤝 貢献ガイド（CONTRIBUTING）

Public Works Stakeholder Map へのご協力ありがとうございます。このプロジェクトは
**公開情報に基づく調査支援ツール**です。データの正しさはコードの正しさと同じくらい重要です。

## 1. 基本方針

- 本システムは「候補の提示」であり、正式な所管・許認可の要否・申請先を断定しません
- 実案件名・個人情報・非公開図面・社内担当者情報をリポジトリへ登録しないでください
- データは国・都道府県・市区町村・警察・管理者の公式情報を正本とし、利用条件を必ず確認します
- 民間まとめサイト・検索結果要約・生成 AI のみで作られた連絡先は正本にしません

## 2. Issue の書き方

機能提案・不具合報告には次を含めてください。

- 対象地域と機関種別
- 現在の表示内容と期待する内容
- 根拠となる公式 URL と確認日
- スクリーンショット（個人情報・案件情報を除く）

連絡先だけを根拠なしに変更する Issue / PR は受け入れられません。

## 3. PR の流れ

1. `main` から目的が分かるブランチを作成（例: `feature/xxx`、`fix/xxx`、`docs/xxx`）
2. 変更に合わせてテストを追加・更新する
3. ローカルで次を全て通す
   - `npm run lint`
   - `npm run typecheck`
   - `npm test`
   - `npm run build`
   - データ変更時: `npm run data:validate`
4. PR 本文に「目的 / 変更内容 / 対象外 / 影響範囲 / テスト結果 / DB変更の有無 / rollback方法」を記載する
5. セキュリティ・免責・データ利用条件・責任境界に関わる変更は重点レビュー対象です

## 4. データ変更のルール

- 情報源の利用条件（著作権・出典表示・再配布条件）が未確認のままデータ本体を追加しない
- スクレイピング結果を無レビューで公開しない（`staging` → レビュー → `published`）
- 推定・欠損・期限超過を削除や null で隠さない
- データ変更は「来歴（出典・取得日時・確認者）」とセットで行う

## 5. セキュリティ

脆弱性の発見は SECURITY.md の手順に従って非公開で報告してください。公開 Issue に
秘密情報・接続文字列・個人情報を投稿しないでください。
