# 🚀 Release Note — v0.4.2（認証本番有効化・実データ整備ツール）

| 項目 | 内容 |
|---|---|
| 🏷️ バージョン | v0.4.2 |
| 📅 リリース日 | 2026-08-05 |
| 🎯 スコープ | 管理 API の認証・RBAC 本番有効化（Issue #34）・実データ整備ツール（Issue #32 第二・三段） |
| 🌐 本番 URL | https://pwsm.mirai-dx-platform.com |

## ✨ 主な変更

- 🔐 **認証・RBAC 本番有効化（#34・PR #47）**: Cloudflare Access アプリ「pwsm」の設定から
  audience / JWKS URL / 管理者メールを自動取得し `AUTH_ENABLED=true` を設定。
  - 管理 API（/admin/*・/audit-events）は Access 経由 + ロール検証が必須に
  - 管理者: `kensan1969@gmail.com`（admin）／ `mirai-const.co.jp` ドメイン利用者は現状 viewer
  - 公開 API は変更なし・Access 迂回の直接アクセスは 401
- 🧱 **実データ整備ツール（#32 第二・三段・PR #48）**:
  - 組織ステージング取込生成（`generate-entity-imports.mjs`・16 件 pending を dev/main 適用済み）
  - N03 行政区域取込ツール（`n03-geojson-to-imports.mjs`）と国土数値情報の台帳登録（`mlit-ksj-n03`）
  - 台帳スキーマ拡張（`region: national`・`format: GML`）

## 🗄️ DB 状態（2026-08-05）

- `provenance.data_sources`: 24 件（実 16 = 代表 3 地域 15 + N03 1、デモ 8・実データは全件 license 付き）
- `staging.import_records`: 16 件 pending（組織レビュー待ち・SCR-07 で二者レビュー）
- migration: 0001 / 0002 適用済み

## 🧪 品質ゲート

- lint / typecheck / build: PASS
- テスト: **194 passed / 6 skipped**（entity seed 3・N03 import 4 を追加）
- CI: quality / dependency audit 全て PASS

## ⚠️ 既知の制限

- ドメイン単位のロール判定は未実装（`mirai-const.co.jp` 利用者は reviewer/editor へ明示追加が必要）
- N03 実データの取得・変換・適用と、窓口情報（office / contact_point）の収集は運用タスクとして残存
- E2E（#35）・OpenAPI（#37）は未着手

## ↩️ Rollback

- Worker: `wrangler versions deploy` で v0.4.1 版 `4f5a9b26-6aa6-4569-9000-22fb771f55d3` へ即時切替
- 認証を無効化したい場合: `AUTH_ENABLED=false` で再デプロイ（従来の 403 ガードへ復帰）
- DB: staging 16 件・N03 台帳行は追加のみのため削除可能（アプリ動作への影響なし）

## 🧪 本番検証メモ（2026-08-05）

- デプロイ version: `13c4359a-96f0-4891-addb-6055bd34ab65`（`wrangler deploy` で AUTH vars を同期）
- 本番スモーク: 公開 API 全て 200 / Access 発行 JWT の検証成功（署名・audience を実キーで確認）
- サービス認証（viewer）で /admin/sources が 403「editor 以上の権限が必要」→ ロール判定の動作確認
- ブラウザでの管理者ログイン（kensan1969@gmail.com → admin 200）は Access セッションでの手動確認対象
