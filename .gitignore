# Dependencies
node_modules/

# Build outputs
dist/
dist-types/
build/
.wrangler/
*.tsbuildinfo

# Environment — never commit real secrets (.env.example only)
.env
.env.*
!.env.example

# Logs / temp
*.log
coverage/
.tmp/

# OS / editor
.DS_Store
Thumbs.db

# Local state (session-scoped, not source of truth)
reports/

# design-sync (staged scripts / build output / machine state)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
